{
    "token": "", //your bot identifier
    "prefix": "!!", //the prefix to use inside the chat
    "appName": "Random Notice Bot", //App Name
    "startNotice": false, //set to true if you whant the bot to send a menssage on start
    "channels":{
        "General": "" //18 digits channel id number required if 'startNotice' == true
    }
}